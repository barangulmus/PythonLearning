numbers=[1, 10, 5, 16, 4, 9, 10]
letters=['a','g','s','b','y','a','s']

    # val=min(numbers) #En büyük kücük
    # val=max(numbers) #En büyük eleman
    # val=min(letters) #En kücük rakamı verir(A-Z)
    # val=max(letters) #En büyük rakamı verir(A-Z)
    # val=numbers[3:6]
    ###################################################################
    # numbers.append(10) # Sıralı giden item listesinde sona ekler.
    # numbers.insert(0,0) #0. elemanı '0' rakamı ile günceller.
    #########################--Eleman Silme--##########################
    # numbers.pop() # Anlamadım ama birşeyleri siliyor.
    # numbers.pop(0) # İlk elemanı siler.
    # numbers.remove(16) # 16 degerini iceren itemi siler.
    ##########################--Sıralama--#############################
    # numbers.sort()
    # letters.sort()
##########################--Ters Cevirme--#############################
    # numbers.reverse()
    # letters.reverse()

print(numbers)
print(letters)